# Kotlin Multiplatform Library (in default mode)

This module shows how to use KtorGen for a KMP library.

This relies on the default mode of KtorGen: *generate a factory top-level function.*

This module is not realistic, but it shows how to use KtorGen with almost all available features.
